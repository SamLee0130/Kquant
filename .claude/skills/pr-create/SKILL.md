# PR Creation Skill
1. Check `gh auth status` and switch account if needed for target repo
2. Stage all changes and create a commit with a conventional commit message
3. Push to current branch
4. Create PR with title matching commit message and body summarizing changes
5. Output the PR URL
