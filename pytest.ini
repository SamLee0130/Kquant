[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Playwright 설정
# --browser chromium: Chromium 브라우저 사용
# --headed: 브라우저 창 표시 (디버깅용, CI에서는 제거)
