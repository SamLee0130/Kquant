[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Playwright 설정
# --browser chromium: Chromium 브라우저 사용 (명령줄에서 지정)
# --headed: 브라우저 창 표시 (디버깅용, CI에서는 제거)
# CI에서는 자동으로 headless 모드로 실행됨
